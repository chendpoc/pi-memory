/** Unix directory mode for agent / socket parent dirs (ignored on Windows). */
export const SECURE_DIR_MODE = 0o700;

/** Unix socket file mode (ignored on Windows). */
export const SECURE_FILE_MODE = 0o600;
