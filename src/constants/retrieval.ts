export const DEFAULT_TOP_K = 3;
export const CANDIDATE_POOL_MULTIPLIER = 3;
export const MMR_LAMBDA = 0.8;
/** Minimum cosine similarity (0–1) for a chunk to enter the candidate pool. */
export const DEFAULT_MIN_RELEVANCE = 0.4;

export const MIN_TOP_K = 1;
export const MAX_TOP_K = 20;
export const MIN_MMR_LAMBDA = 0;
export const MAX_MMR_LAMBDA = 1;
