---
"@chendpoc/pi-memory": minor
---

Add offline Write → Consolidate → Retrieve loop: session enqueue on shutdown, stage1/phase2 pipeline, MEMORY.md indexing, OS scheduler, and `/memory --verbose` diagnostics.
